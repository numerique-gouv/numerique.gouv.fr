import { assert }from 'chai'

describe('geo', function() {
  describe('#get_coordinates()', function() {
    it('Should call coordinates service', function () {

      // GIVEN

      // WHEN
      // THEN
      assert(false);
    });
  });
});
